<h1><%= t ".users" %></h1>

<% @users.each do |st| %>
  <div>
    <%= st.code %>
    | <%= st.username %>
    | <%= link_to t(".delete"), st, method: :delete,
      data: { confirm: t(".sure") } %>
  </div>
<% end %>
<%= will_paginate %>
